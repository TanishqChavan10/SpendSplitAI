{
  "name": "mumbai-hacks-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SplitSphere - AI-powered group expense tracking app",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "concurrently \"npm run start --workspace=frontend\" \"npm run start --workspace=backend\"",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "clean": "npm run clean --workspace=frontend && npm run clean --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "expense-tracker",
    "split-bills",
    "group-expenses",
    "nextjs",
    "monorepo"
  ],
  "author": "SinghAman21",
  "license": "MIT"
}